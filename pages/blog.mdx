---
type: posts
description: Hey, I'm Aidos! I'm a software developer from Kazakhstan. Welcome to my spot on the web for my projects, tutorials, blogs and anything else I want to show the world.
ogImage: /og.png
---

import { getPagesUnderRoute } from "nextra/context";
import Link from "next/link";

export function BlogHeader() {
  return (
    <div className="max-w-screen-lg mx-auto pt-4 pb-8 mb-16 border-b border-gray-400 border-opacity-20">
      <div className="text-center leading-snug text-5xl mt-4 mb-16 nx-text-slate-900 dark:nx-text-slate-100">Blog</div>
      <div className="text-center text-gray-500 dark:text-gray-400 font-space-grotesk">
        Find the latest of my writing here.
      </div>
    </div>
  );
}

export function BlogIndex() {
  return getPagesUnderRoute("/blog").map((page) => {
    return (
      <div key={page.route} className="mb-10">
        <Link
          href={page.route}
          style={{ color: "inherit", textDecoration: "none" }}
          className="block font-semibold mt-8 text-2xl"
        >
          {page.meta?.title || page.frontMatter?.title || page.name}
        </Link>
        <div className="opacity-80 mt-10">
          {page.frontMatter?.description?.slice(0, 200)}
          {" ... "}
          <Link href={page.route}>
            <span className="inline-block font-semibold decoration-dotted decoration-2 underline underline-offset-4 hover:no-underline">
              {"Read more →"}
            </span>
          </Link>
        </div>
        {page.frontMatter?.date ? (
          <div className="opacity-50 text-sm mt-10">
            {page.frontMatter.date}
          </div>
        ) : null}
      </div>
    );
  });
}

<BlogHeader />
<BlogIndex />
